<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mindpulse.backend.mapper.AuditLogMapper">

    <resultMap id="AuditLogResultMap" type="com.mindpulse.backend.entity.AuditLog">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="action" column="action"/>
        <result property="resourceType" column="resource_type"/>
        <result property="resourceId" column="resource_id"/>
        <result property="ipAddress" column="ip_address"/>
        <result property="userAgent" column="user_agent"/>
        <result property="details" column="details"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="findById" resultMap="AuditLogResultMap">
        SELECT * FROM audit_logs WHERE id = #{id}
    </select>

    <select id="findByUserId" resultMap="AuditLogResultMap">
        SELECT * FROM audit_logs WHERE user_id = #{userId} ORDER BY created_at DESC
    </select>

    <select id="findWithFilters" resultMap="AuditLogResultMap">
        SELECT * FROM audit_logs
        <where>
            <if test="userId != null and userId != ''">
                AND user_id = #{userId}
            </if>
            <if test="action != null and action != ''">
                AND action = #{action}
            </if>
            <if test="resourceType != null and resourceType != ''">
                AND resource_type = #{resourceType}
            </if>
        </where>
        ORDER BY created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countWithFilters" resultType="int">
        SELECT COUNT(*) FROM audit_logs
        <where>
            <if test="userId != null and userId != ''">
                AND user_id = #{userId}
            </if>
            <if test="action != null and action != ''">
                AND action = #{action}
            </if>
            <if test="resourceType != null and resourceType != ''">
                AND resource_type = #{resourceType}
            </if>
        </where>
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO audit_logs (user_id, action, resource_type, resource_id, ip_address, user_agent, details)
        VALUES (#{userId}, #{action}, #{resourceType}, #{resourceId}, #{ipAddress}, #{userAgent}, #{details})
    </insert>

    <select id="countByDate" resultType="int">
        SELECT COUNT(*) FROM audit_logs WHERE DATE(created_at) = #{date}
    </select>

</mapper>
